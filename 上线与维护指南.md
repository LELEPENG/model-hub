# model-hub 上线 & 维护指南

> 一句话结论：**不买域名也能上线，维护几乎为零，全程免费。**
> 这个站是纯静态的（HTML + CSS + JS，没有后端、没有数据库、没有构建步骤），
> 所以「上线」= 把文件传到某个能存放静态文件的地方，仅此而已。

---

## 一、先弄清一件事：要不要买域名？

**不用。三种方式都送免费二级域名。**

| | 免费二级域名 | 需要备案吗 | 国内访问 |
|---|---|---|---|
| Cloudflare Pages | `xxx.pages.dev` | 不需要（境外节点） | 中等 |
| GitHub Pages | `xxx.github.io` | 不需要（境外节点） | 偏慢/偶发不通 |
| 国内云（OSS / COS / EdgeOne） | 无，必须自备域名 | **必须 ICP 备案** | 最快 |

### 什么时候才值得买域名
- 只是自己用 / 发给朋友：**不用买**，二级域名足够。
- 想做品牌、印在名片上、显得正式：买一个。`.top` / `.xyz` 首年几块钱，`.com` 约 60–80 元/年。
- **关键坑**：只要你的站点放在 Cloudflare Pages / GitHub Pages（境外节点），
  就算绑了自定义域名也**不需要备案**。只有换到国内服务器才需要备案（周期 10–20 天）。
- 建议路径：**先用免费二级域名跑起来 → 觉得值得了再买域名 → 在 Pages 后台点几下绑上去**。
  域名绑定随时可加，不需要重新部署。

---

## 二、三种上线方式（按推荐度排序）

### 方式 A：WorkBuddy 资料库（最省事，国内访问快）
- **优点**：零配置、国内直连快、这个站之前就发过一次（页面里还带着 `data-page-node-id` 标记）。
- **限制**：只吃**单个 HTML 文件**。
- **操作**：
  1. 用已经生成好的 `dist/model-hub.html`（见第四节）
  2. 让 WorkBuddy 调用「资料库」技能上传即可
- **适合**：想最快拿到一个能分享的链接。

### 方式 B：Cloudflare Pages（推荐，最稳、功能最全）
- **优点**：免费额度极大（无限请求 / 每月 500 次构建 / 带宽不限量级）、
  支持直接拖整个文件夹（**不用打包**）、自动 HTTPS、全球 CDN、可绑自定义域名。
- **操作**：
  1. 注册 cloudflare.com（免费，不用信用卡）
  2. Workers & Pages → Create → Pages → Upload assets
  3. 把 `J:\WorkBuddy\2026-09-07-23-25-37\model-hub` 整个文件夹拖进去
     （或只拖 `dist/model-hub.html` 单文件也行，改名为 `index.html`）
  4. 拿到 `xxx.pages.dev` 链接，完事
- **更新**：后台重新 Upload assets 覆盖即可，或连 GitHub 仓库自动部署。

### 方式 C：GitHub Pages（免费，但国内访问一般）
- 建一个公开仓库，把文件推上去，Settings → Pages → 选分支根目录
- 得到 `https://用户名.github.io/仓库名`
- 国内访问速度和稳定性不如 Cloudflare，作为备选。

> **我的建议**：先走方式 A（最快拿到链接），如果后面要长期运营/绑域名，迁到方式 B。
> 两个方式都不花钱，迁移成本 = 重新传一次文件。

---

## 三、后期维护：要做什么、多少钱

### 钱：0 元
- 静态托管免费额度对这个站（175 KB）来说绰绰有余——
  就算每月 1 万人访问，也就 1.75 GB 流量，连免费额度的零头都不到。
- 没有服务器要养、没有数据库要备份、没有依赖会过期（站点零第三方库）。

### 事：只有一件 —— **更新数据**
模型世界一个月一代，站点的价值全在数据新鲜度上。三类更新：

| 类型 | 频率 | 改哪里 |
|---|---|---|
| 排名变化 | 每月 / 每季度 | `data.js` 顶部的 `TOP20` 常量（改 rank / score / pros / best / avoid） |
| 模型增删 | 有新模型时 | `data.js` 的 `MODELS` 数组加条目 → 再挂进 `TIERS[].picks` 和 `NEEDS[]` |
| 价格 / 上下文变动 | 随缘 | 对应模型条目的 `price` / `ctx` / `updated` 字段 |

改完记得把 `DATA_VERSION` 和该条目的 `updated` 一起改掉，页面底部会显示核实日期。

### 唯一需要人工介入的：实验室工单
- 站点是纯静态的，**访客提交的留言只存在他自己的浏览器里**，不会自动到你手上。
- 流程：访客点「导出 JSON」→ 发给你 → 你在实验室页面**连续按两次 `O`** 进站长模式 →
  「导入 JSON 工单」→ 实测后回帖 → 再导出让他刷新。
- 这是设计上的取舍（不要后端 = 不要服务器成本），如果你觉得麻烦，
  可以后期把工单改成接一个免费的表单服务（如 Formspree 免费档）。

---

## 四、已经给你准备好的东西

| 文件 | 作用 |
|---|---|
| `dist/model-hub.html` | **单文件成品**（175 KB），CSS/JS 全部内联，双击就能在本地打开，可直接上传 |
| `build_standalone.py` | 一键打包脚本。改完 `data.js` 后跑一次，重新生成上面的单文件 |

### 打包命令
```bash
cd J:\WorkBuddy\2026-09-07-23-25-37\model-hub
python build_standalone.py
```
脚本会自动：内联 CSS 与 5 个 JS、去掉开发用的 `?v=` 版本号、
加上静态托管需要的 `data-sp-mode="scroll"` 根标记，并做三项自检
（是否残留未内联的本地资源 / JS 里是否有会截断的 `</script` / CSS 是否替换成功）。

### 唯一外部依赖
只有 Google Fonts 的镜像 `fonts.font.im`（Outfit / Noto Sans SC / JetBrains Mono）。
**字体加载失败也不影响任何功能**，会自动回退到系统字体，只是没那么好看。

---

## 五、更新上线的标准流程

```
1. 改 data.js（排名 / 模型 / 价格）
2. 跑 python build_standalone.py
3. 本地打开 dist/model-hub.html 确认没白屏
4. 上传：
   - 资料库  → 上传 dist/model-hub.html
   - Cloudflare → 拖文件夹 / 或拖 dist/model-hub.html（改名 index.html）
5. 让朋友用手机开一次，确认移动端正常
```

---

## 六、几个可能想问的

**Q：访客数据会不会泄露？**
不会。站点不上传任何数据，工单只存在访客自己浏览器，实验室页面里也写明了这点。

**Q：手机上能用吗？**
能。排行榜、叠卡引导、模型卡都有移动端断点（860px / 560px），
离子粒子数在触屏设备上会自动减半以省电。

**Q：以后想加统计（多少人访问）怎么办？**
Cloudflare Pages 自带免费访问统计（Web Analytics，不侵入、不用挂 Google Analytics）。
资料库方式没有统计。

**Q：能挂载自己的域名吗？**
Cloudflare Pages 可以，后台 Custom domains 里加一条，按提示改一条 CNAME 即可，5 分钟搞定。
