# model-hub 网站上线方案（2026 版 · 修订稿）

> 适用范围：本项目是一个**单文件静态站点**（`dist/index.html`，约 200.8 KB，纯 HTML/CSS/JS，无后端、无数据库），由 `build_standalone.py` 打包，随附 `llms.txt` / `robots.txt` / `sitemap.xml` / `edgeone.json` 部署脚手架。
> 目标：正式对外发布，做到合规、可被发现（含 AI 搜索）、可长期维护、并具备清晰的盈利路径。
> 约束：优先使用**免费**托管与服务（Cloudflare Pages / GitHub Pages / EdgeOne 免费套餐 / WorkBuddy 资料库），不主动引入付费方案。
> 说明：本方案仅作规划，每一步后续由你逐一确认后再执行。本章已根据 2026 年最新合规与 SEO 实践修订，重点修正了「ICP 备案与免费托管的冲突」「JS 渲染对 AI 搜索收录的致命风险」两处关键认知。

---

## 一、上线前需完成的具体事项与准备清单

### 1.1 站点内容与质量自检
- [ ] **内容完整性**：确认 Top 50 榜单、各模型详情、封面/钩子文案均已填充，无占位符、无 "TODO"、无样例数据残留。
- [ ] **日期与版本**：确认 `DATA_VERSION`、榜单日期（如 `2026.09` / `2026-09-11`）为最新发布日，且全站一致。
- [ ] **链接有效性**：所有模型外链、官网/文档链接可访问（避免 404 死链影响 SEO 与信任）。
- [ ] **文案与版权**：确认无未授权抓取内容、无侵权图片/字体；若引用第三方数据需注明来源与时间。
- [ ] **移动端适配**：在手机宽度（≤375px）下检查排版、字体、按钮点击区是否正常。
- [ ] **数据来源声明 + 免责声明（新增）**：榜单为**编辑/观点性排名**，非官方结论；在页脚明确「数据截至日期、来源、仅供参考、不构成投资/选型最终建议、各模型商标归各自所有者」。可显著降低不正当竞争/名誉权纠纷风险。

### 1.2 性能与体验（Core Web Vitals 门槛，2026 标准）
- [ ] **LCP ≤ 2.5s**：单文件已内联 CSS/JS，避免额外请求；建议启用托管平台的 Brotli/Gzip 压缩。
- [ ] **INP ≤ 200ms（2026 关键）**：INP 已于 2024-03 取代 FID，衡量**整个会话**每次交互的响应延迟，是 2026 年**最难达标**的指标。本站的离子动画、筛选、滚动等主线程长任务会拖累 INP → 需：拆分长任务、交互时 `yield` 主线程、避免动画在主线程计算（已用 GPU/`lighter` 合成，继续保持）、为非必要脚本加 `font-display: swap`。
- [ ] **CLS ≤ 0.1**：图片/字体预留尺寸，避免布局抖动。
- [ ] **TTFB ≤ 800ms（诊断指标）**：虽非核心指标，但决定 LCP 下限；静态托管通常轻松达标。
- [ ] **无障碍 / WCAG 2.2 AA（新增）**：为图片加 `alt`、保证文字对比度 ≥ 4.5:1、键盘可聚焦、可见焦点样式、避免纯色块传达信息。AI 与搜索引擎均将可访问性视为质量信号。
- [ ] **总体积**：191 KB 属轻量，但需确认无超大内联资源（如 base64 大图）；必要时外链或压缩。

### 1.3 基础 SEO 与"AI 可读性"（AEO / GEO，2026 关键）
> 2026 搜索范式已从「SEO 排名」演进到「SEO → AEO（答案引擎优化）→ GEO（生成式引擎优化）」三层。AI 搜索（ChatGPT 依赖 **Bing 索引 + GPTBot**、Perplexity 实时检索、Google AI Overviews）正快速吞噬传统点击流量，必须针对性优化。

- [ ] **结构化数据 JSON-LD**：为榜单/模型详情添加 `ItemList` / `Product` / `FAQPage` / `Organization`，并放进**首屏初始 HTML**（不要等 JS 注入），用 Google Rich Results Test 校验。
- [ ] **llms.txt（新增）**：在站点根目录放一个 `llms.txt`（2024 提出的纯文本标准，成本极低），告诉 LLM 本站有哪些内容、哪些是重点，提前卡位 AI 抓取。
- [ ] **答案块前置**：在每段标题下方放 40–60 字的直接答案（AI 优先抽取这类"答案块"），用问答式标题（What/How/为什么）。
- [ ] **对比表 + 原创数据**：AI 极爱引用**结构化对比表**与**原创统计**——本榜单本身就是原创排名数据，应在页面显式标注「数据来源：model-hub 实测/整理，截至 2026.09」，主动成为被引用源。
- [ ] **语义化标题与描述**：每个区块有清晰 `<h1>`、`<meta description>`、`<title>`，且置于初始 HTML。
- [ ] **FAQ 区块**：针对"哪个模型最强""2026 性价比模型""国产模型排行"等高频问题，提供可被 AI 直接抓取的简短问答。
- [ ] **robots.txt**：允许抓取，**切勿** `Disallow` 站点的 `.js`/`.css`（Google 需要它们才能渲染）；上线前移除 `noindex`。
- [ ] **XML 站点地图**：生成 `sitemap.xml` 并提交。
- [ ] **实体一致性（新增）**：站点名 model-hub、作者名在各平台保持一致，配 `Organization` schema，强化 AI 知识图谱中的品牌实体识别。

### 1.4 ★ 技术 SEO 致命点：JS 渲染内容对 AI 搜索不友好（新增，务必重视）
- **现状风险**：本站的 Top 50 榜单由 `data.js` + `app.js` 在浏览器端**运行时生成 DOM**。Google 虽能渲染 JS，但采用「两波索引」——先抓空壳 HTML，再把 JS 放入渲染队列，**延迟数小时到数天**；而 **Bing / Perplexity / 多数 AI 爬虫对 JS 的处理弱于 Google**。由于 AEO/GEO 高度依赖 Bing（ChatGPT）与 Perplexity，这意味着**你的核心榜单很可能不被 AI 搜索及时/完整收录**。
- **修复方案（任选其一，强烈建议做）**：
  1. **构建期预渲染（推荐）**：修改 `build_standalone.py`，在打包时把 Top 50 列表**直接写成静态 HTML** 注入到单文件里（而不是仅靠 JS 运行时生成）。JS 仍负责交互，但初始 HTML 已含完整榜单 → 所有爬虫零延迟可见。
  2. **`<noscript>` 兜底 + JSON-LD 全量数据**：在 `<noscript>` 内输出纯文本/静态榜单，并在 JSON-LD 中完整列出 50 条数据，保证无 JS 环境也能被索引。
  3. **自查方法**：用 Search Console「网址检查 → 实测网址 → 查看已呈现的 HTML」，或 Chrome DevTools 关掉 JS 看页面是否为空——为空即确认中招。
- 这是本次修订中**最关键**的一条技术补充。

### 1.5 合规前置（面向中国访问者）
- [ ] **ICP 备案（非经营性）—— 与免费托管的冲突必须澄清**：
  - 备案的判断标准是**服务器物理位置**，不是域名后缀。域名解析到**中国大陆服务器**才必须备案；解析到**香港/海外**则**无需备案**。
  - **关键现实**：ICP 备案**必须通过国内接入商（阿里云/腾讯云/华为云等持有增值电信许可证的厂商）提交**，且需配套国内服务器/对象存储。**Cloudflare Pages、GitHub Pages 等海外托管无法做 ICP 备案**。
  - **与"免费铁律"的冲突**：要"中国大陆高速访问 + 合规备案" → 必须购买国内服务器/静态托管（**非免费**）；要"全程免费" → 只能走**免备案的海外/香港节点**，代价是国内访问速度与稳定性下降。二者不可兼得，需你决策（见 2.2）。
  - 若走备案路径：个人备案需**域名实名认证（WHOIS 与备案主体一致）**、身份证、人脸核验，部分省份要求域名注册满 3 个月；网站名称不得含"中国/中华"等；流程约 3–22 个工作日（初审 1–2 天 + 短信核验 + 管局 10–20 天）。备案免费、无年费。
- [ ] **算法备案**：仅当提供"具有舆论属性或社会动员能力"的算法推荐（个性化推送/排序精选/检索过滤/生成合成/调度决策）时才需备案，周期 2–3 个月，建议上线前完成。本项目**纯静态展示榜单、无个性化推荐**，通常**不在范围内**；但若后续加入"智能排序/猜你喜欢/个性化推荐"即触发。
- [ ] **大模型备案 / 登记**：
  - **大模型备案**：仅当**自研或实质二次训练大模型**对外提供生成式 AI 服务才需（省级初审 + 国家网信办终审，周期 6–8 个月）。
  - **大模型登记**：仅当**调用第三方已备案大模型 API** 对外提供生成式 AI 服务（省级网信办审核，材料较简）。
  - 本项目**仅展示第三方模型数据、自身不训练也不调用大模型生成内容** → **两者均不需要**。若未来接入 AI 问答/生成功能，再按"API 调用=登记 / 自训=备案"补齐。
  - **前瞻提示**：若未来在站内发布 AI 生成内容（图文/视频），须遵守《人工智能生成合成内容标识办法》（2025-09 施行），添加显式/隐式标识。
- [ ] **个人信息保护（PIPL）与数据出境（2026 趋严）**：
  - 2026-01-01 起，《网络安全法》修订版施行、数据出境认证办法生效；违规罚款上限**5000 万元或上年营业额 5%**，责任人个人罚款最高 100 万元，"警告期"大幅缩短。
  - **Google Fonts / Google Analytics 风险**：德国慕尼黑法院已于 2022 年裁定通过 Google 服务器加载字体违反 GDPR（未经同意传输用户 IP）——该判例被全球广泛引用。在中国法域下，向境外传输用户 IP 等个人信息同样触碰 PIPL 数据出境红线。
  - **硬性建议**：移除 Google Fonts，改用**系统字体栈**（`-apple-system, "PingFang SC", "Microsoft YaHei", sans-serif`）或**自托管字体 / Bunny Fonts（欧盟、零追踪）**；统计用**国内合规工具（百度统计/友盟+）**或自托管 Matomo。**不要**为省事保留境外分析。
  - 注意：即使 Cloudflare 的海外 CDN，若处理中国用户个人信息也存在数据出境考量；面向国内时优先国内/香港节点。
- [ ] **隐私政策页**：即便仅做统计，也建议发布简易《隐私政策》，说明数据收集范围与用途、是否跨境。

### 1.6 域名与托管（免费路径，修订）
- [ ] **域名（修正"免费域名"误区）**：真正长期可靠的**免费顶级域名极少且常被收回/不可信**（如旧 `.tk/.ml`）。务实选择：①直接用平台免费子域（`xxx.pages.dev` / `xxx.github.io`，永久免费）；②在阿里云/腾讯云等以约 **50–80 元/年**（新人常低至 1 元/年）注册 `.com/.cn` 等便宜域名。**不要**迷信"免费域名"。
- [ ] **托管选型（全免费，并标注国内可达性）**：
  1. **Cloudflare Pages**：无限带宽/请求、自动 HTTPS、全球 CDN、Brotli、永久免费、免备案；**缺点：国内三大运营商访问常"抽风"**。最利于海外与 AI 搜索收录。
  2. **GitHub Pages**：1 GB 存储、月 100 GB 流量软上限、免费 SSL、免备案；**缺点：国内访问慢、功能少**。利于开发者群体与 Google 收录。
  3. **EdgeOne（腾讯云国际站）免费套餐（新增推荐）**：不限流量、免费 DDoS/WAF、免备案、回源智能锁**香港/新加坡**节点 → **国内访问明显优于纯 Cloudflare/GitHub**，且可"套"在 Cloudflare 站外层做双线路互备。注册仅需邮箱、无需实名。适合"想免费又要国内能开"的场景。
  4. **WorkBuddy 资料库**：仅面向国内/工作流内分发时，直接发布为分享链接，零运维。
- [ ] **自定义域名 + HTTPS**：绑定域名后开启强制 HTTPS 与 HSTS；**EdgeOne 用 `edgeone.json` 的 `headers` 配置安全响应头**（Cloudflare 才用 `_headers` 文件）。
- [ ] **301 重定向表**：若发布后 URL 结构变化，准备新旧路径 301 映射，保住已有权重。

### 1.7 监测与分析（合规优先）
- [ ] **分析工具**：首选国内合规统计（百度统计/友盟+）；若面向海外且接受合规提示，可用 **GA4 + Consent Mode v2**（需弹窗征求同意）。
- [ ] **收录提交**：上线后向 **Google Search Console** 与 **Bing Webmaster Tools** 提交 sitemap（Bing 是 2026 年 ChatGPT 搜索的重要索引源，务必重视）。
- [ ] **RUM 实时监控（新增）**：用 `web-vitals` JS 库把 LCP/INP/CLS 真实字段数据上报，捕捉部署后的 INP 劣化（比等 28 天 CrUX 窗口更早发现问题）。

### 1.8 回滚与备份
- [ ] **保留可回滚版本**：每次发布保留上一个 `dist/index.html` 副本，设置 **72 小时回滚窗口**（EdgeOne 控制台也可一键回退历史部署）。
- [ ] **源码归档**：确保 `index.html / styles.css / data.js / app.js` 等源文件在 Git 或资料库有版本记录。

---

## 二、上线过程中的关键注意事项与风险点

### 2.1 发布顺序（建议）
1. 先在**临时/预览环境**（如 Cloudflare 预览分支、GitHub Pages 草稿）跑一遍真实访问。
2. 用无痕窗口 + 多设备（PC/手机）实测渲染、筛选、滚动、外链。
3. 确认无 JS 报错（浏览器控制台）、无混合内容（HTTP 资源嵌 HTTPS 页）。
4. 切换正式域名/CNAME，开启 HTTPS 强制跳转。
5. 提交 sitemap，提交收录。

### 2.2 ★ 合规与访问速度的取舍（核心风险，已修订）
- **备案 vs 免备案 的本质冲突**：
  - 想要「中国大陆高速 + 合规」→ **必须 ICP 备案 + 国内服务器/托管（非免费）**，且备案需通过国内接入商、配套国内资源。
  - 想要「全程免费」→ 只能 **免备案 + 海外/香港节点**（Cloudflare / GitHub / EdgeOne 国际站），代价是国内访问易波动。
  - 二者不可兼得。**不存在"免费且备案"的静态托管**。
- **建议**：
  - 若目标用户含国内从业者且愿意付费做国内托管 → 走备案 + 国内 COS/OSS 静态托管 + 国内 CDN。
  - 若坚持免费 → 用 **EdgeOne 免费套餐（国内可达性较好）** 或 Cloudflare Pages（全球快、国内抽风）；接受国内访问波动。
  - 若主打全球/开发者 → Cloudflare Pages 免备案即可。

### 2.3 数据出境风险（PIPL，已强化）
- 使用 Google Fonts/Analytics = 用户 IP 等数据出境，2026 年监管趋严（网络安全法修订 2026-01-01、数据出境认证办法 2026-01-01、罚款上限 5000 万/5%）。
- **硬性建议**：移除 Google Fonts，改用系统字体栈或自托管/Bunny Fonts；统计用国内工具。**不要**为省事保留境外分析。

### 2.4 单文件站点的特殊性
- 单 HTML 内联全部资源 → **无构建依赖、部署极简**，但也意味着每次改榜单都要重新生成整文件。
- 风险：编辑时并行多改易产生"覆盖丢失"（本项目已多次踩坑）。**发布前用脚本校验**：50 个排名唯一、分数严格递减、字段齐全、无残留旧标签（如 `Top20`）。

### 2.5 ★ JS 渲染导致 AI 搜索漏收（技术风险，已单列 1.4）
- 核心榜单靠 JS 运行时生成 → Google 两波索引延迟、Bing/Perplexity 处理更弱 → ChatGPT 等 AI 搜索可能**看不到你的榜单**。
- **上线前务必按 1.4 做预渲染或 noscript+JSON-LD 兜底**，否则 AEO/GEO 努力基本白费。

### 2.6 内容时效风险
- 模型榜单半衰期短，发布日即"开始过时"。需在首页明确"数据截至 2026.09"，并规划定期更新节奏（见第三部分）。

### 2.7 SEO/AI 收录踩坑
- 上线前若残留 `noindex` / staging 密码 / `Disallow: /` → 永不收录；且**切勿** `Disallow` 自己的 `.js`/`.css`。
- AI 搜索更依赖**结构化数据 + 清晰 FAQ + 答案块 + llms.txt**，缺这些会显著降低被引用概率。

### 2.8 法律边界
- 不发布未核实的模型参数/跑分，避免虚假宣传风险；引用数据标注来源与时间。
- 不外链违规/侵权内容；用户生成内容（若有评论）需有审核与免责声明。
- 排名类站点务必加**免责声明**（见 1.1），避免名誉权/不正当竞争争议。

---

## 三、上线后的持续维护策略与运维规划

### 3.1 内容更新节奏
- **榜单**：每月至少一次校对 Top 50 分数与新模型（可设**每月 1 号**为固定更新日）。
- **重大发布**：遇头部模型大版本（如 GPT / Gemini / Claude / 文心 / 豆包等）即时补录。
- **更新流程**：改 `data.js` → 跑 `build_standalone.py`（含 1.4 的预渲染）重新生成单文件 → 预览校验 → 发布 → 保留上一版备份。

### 3.2 技术运维（轻量）
- **可用性监控**：用免费 uptime 工具（如 UptimeRobot 免费档）监控域名可达性，异常时邮件/微信提醒。
- **CDN/缓存**：确认托管平台缓存策略；发布后必要时"清缓存 / purge"使新文件生效（EdgeOne 免费套餐每日手动清缓存仅 10 次，需规划）。
- **HTTPS 证书**：托管平台通常自动续期；定期检查不过期。
- **安全响应头（新增）**：通过 `edgeone.json`（`dist/` 内）配置 CSP / X-Content-Type-Options / Referrer-Policy / X-Frame-Options / Permissions-Policy，降低 XSS 与信息泄露面。
- **依赖零运维**：因无后端/数据库，主要运维成本集中在"内容更新 + 合规核验"。

### 3.3 数据与合规续期
- **ICP 备案**：若已备案，注意年审/信息变更及时更新，避免被注销。
- **隐私政策**：随功能变化（如新增统计/评论）同步更新。
- **定期合规体检（每季度）**：复查是否误引入境外脚本、是否新增需备案的算法功能、是否有 AI 生成内容需加标识。

### 3.4 增长与收录维护（AEO/GEO）
- 持续向 Search Console / Bing 提交更新后的 sitemap；配置 `web-vitals` RUM 监控 INP 回归。
- **每月手动测试 10–20 个核心问题**在 ChatGPT / Perplexity / Gemini / Bing 的引用情况（2026 关键动作）。
- 维护 FAQ、答案块、对比表与原创数据，提升"被 AI 直接回答"的概率。
- **同步运营社媒分发**：你已有封面工厂（小红书/抖音/视频号/B站/公众号多尺寸），把榜单做成图文/短视频导流，是零成本获取国内流量的主阵地。

### 3.5 备份策略
- 源码（Git/资料库）+ 每次发布的 `dist` 单文件，至少保留最近 3 个历史版本，便于回滚与审计。

---

## 四、网站可行的盈利模式与商业化路径（已修订）

> 原则：**先流量、后变现**。在月访问量与用户信任建立前，重广告会损害体验。以下按"轻→重"排序，并标注与「备案/托管」决策的耦合关系。

### 4.1 轻量起步（0 成本、可立即做）
- **联盟/导购（Affiliate）**：为榜单中的模型/云服务（各厂 API、云 GPU 实例）加带返利链接；用户通过你的链接开通即获佣金。
  - 国内：**淘宝联盟 / 京东联盟**（CPS 1%–50%，门槛低，适合所有站长）。
  - 海外：**Amazon Associates / CJ / ShareASale**（SaaS 佣金常 20%–50%，订阅制可持续分润）。
- **赞助/友情链接**：接受相关合规厂商的"官方入口"赞助位（需明确标注"赞助"，保持编辑独立）。
- **打赏/赞助**：页脚加"请作者喝咖啡"（爱发电 / 支付宝/微信赞赏），适合早期口碑积累。

### 4.2 知识付费与数字产品
- **付费榜单/深度报告**：免费看 Top 50 概览，付费看"横向评测、选型指南、行业解读 PDF"。
- **模板/工具包**：如"模型选型决策表""提示词模板"等可下载数字商品（Gumroad / 知识星球 / 小报童等分发）。
- 平台参考：小报童（订阅制，抽成约 5%–10%）、知识星球（抽成 20%，个人低门槛）、Patreon/Substack（海外）。

### 4.3 服务变现（与你的主业高度契合）
- **代部署/建站服务**：你已跑通静态站上线全流程，可将"帮人把榜单/作品集/产品页上线（含备案指导）"作为付费服务 —— 直接复用本项目经验。
- **AI 应用咨询**：结合你在 ComfyUI / 大模型应用上的实战，提供 1v1 咨询或小班课。

### 4.4 广告（流量成熟后，注意与备案耦合）
- **⚠️ 关键耦合**：**百度联盟等国内广告联盟审核严格、通常要求网站已 ICP 备案**；若你选择"免备案 + 海外托管"路径，国内联盟基本不可入。**AdSense 走海外、无硬性流量门槛但需内容质量审核**（RPM 约 $3–$10）。
- 进阶广告网络（Mediavine/Raptive/Ezoic）有流量门槛（2.5 万–5 万月会话）且多需海外主体。
- 建议：免备案阶段用 AdSense；若走备案+国内托管，再叠加百度联盟。广告脚本同样触发数据出境/备案考量，接入前对照 1.5/2.3 评估。

### 4.5 会员/订阅（高信任后）
- 提供"会员专享"：抢先评测、闭门社群、定制榜单导出等，按月/年订阅（知识星球/Patreon）。

### 4.6 进阶（长期）
- **网站整体转让（Flipping）**：若做出稳定被动流量，可作为数字资产出售。
- **AI 增强功能**：在合规完成备案/登记后，接入"AI 选型助手"等增值功能，开辟 SaaS 化收入（届时需补算法备案/大模型登记）。

### 4.7 商业化红线提醒（强化）
- 凡涉及"推荐/导购/广告"，须遵守《广告法》与平台规则：**明示"广告/赞助"**，禁用未证实的**绝对化用语**（"最""第一""首选"等无依据表述），不夸大、不误导。
- 国内盈利（联盟/广告）若绑定域名与备案主体，注意主体资质与税务合规（即便是个人）。
- 联盟链接必须**真实使用过/了解**再推荐，否则转化与信任双输，且易被搜索引擎降权。

---

## 附：推荐的上线执行顺序（待你逐条确认）
1. **决策**：托管方案（Cloudflare Pages / GitHub Pages / EdgeOne 免费 / 资料库）+ 是否备案（备案=付费国内托管，免备案=免费海外/香港）。
2. **技术 SEO 硬改（最高优先）**：按 1.4 做榜单预渲染 / noscript + JSON-LD 兜底 + 加 `llms.txt`。
3. 合规改造：去 Google Fonts、加国内统计、补隐私政策与免责声明。
4. 加 JSON-LD 结构化数据 + sitemap + robots.txt（勿屏蔽 js/css）。
5. 预览环境实测（多设备、关 JS 验证榜单可见、无 JS 报错、INP 监控）。
6. 正式发布 + 强制 HTTPS + 提交 Search Console / Bing 收录。
7. 配置监控（uptime + web-vitals RUM）+ 制定月度更新节奏 + 社媒分发。
8. 流量稳定后，按 4.1→4.3→4.4 顺序开启变现（注意 4.4 与备案的耦合）。

> **决策进展（2026-09-11）**：第 1 步「托管与备案决策」已确定 —— 选用 **EdgeOne Pages 国际版（腾讯云国际，免备案、回源香港/新加坡、大陆访问较稳、完全免费）**，与「免费优先、不碰付费与备案」铁律一致。第 2–7 步的技术 SEO 硬改与代码优化已完成（见附录 D）。**正式发布的实操步骤见《EdgeOne部署指南.md》**。剩余「正式发布（按指南执行）/ 提交收录 / 开启变现」待你确认后推进；其中百度联盟强制要求备案，与免备案取向冲突，变现阶段将优先选用不强制备案的渠道（淘宝/京东联盟、赞助、打赏）。

---

## 附录：评审、修订与本轮已落地的代码优化（2026-09-11）

> 本附录将《上线方案_评审与补充.md》的问题清单、补充建议统一并入本方案，并补充本轮回填到代码的实际改动，使本文件成为唯一、完整的上线交付文档。原独立评审文件已合并后撤销。

### 附录 A. 需注意的问题清单（原文档错漏 + 风险点）
1. **【严重·已修正】"免费 + ICP 备案"被写成可共存**：ICP 备案必须经由国内接入商并提交国内服务器，海外托管无法备案；"免费"与"备案+大陆高速"本质冲突。
2. **【严重·已修正】JS 渲染导致 AI 搜索漏收**：Top 50 原由 JS 运行时生成 DOM，Bing/Perplexity/AI 爬虫处理 JS 弱于 Google → 核心榜单可能不被 AI 搜索收录。本轮回填修复（见附录 D）。
3. **【中等·已修正】"免费域名"误导 / 百度联盟门槛未说清**：国内联盟通常要求已备案；免备案路径下不可入。
4. **【中等·已修正】第三方字体数据出境**：原站引用 `fonts.font.im`（境外字体 CDN），触碰 PIPL 红线且阻塞渲染 → 本轮回填改用系统字体栈，零外部依赖。
5. 持续警惕项：PIPL 出境、单文件并行编辑覆盖丢失、内容时效、noindex/Disallow 误留（新增"勿屏蔽 js/css"）。
6. 原缺失现已补：llms.txt、WCAG 2.2 无障碍、web-vitals RUM、数据来源+免责声明、安全响应头、生成式AI内容标识办法（前瞻）、EdgeOne 免费套餐、分层变现、广告法绝对化用语。

### 附录 B. 内容补充建议（落地动作清单）
- **S1（已落地）**：榜单预渲染 / noscript + JSON-LD 兜底，由 `build_standalone.py` 自动从 `data.js` 的 TOP50 生成。
- **S2（已落地）**：添加 `llms.txt` / `robots.txt` / `sitemap.xml` / `edgeone.json`（EdgeOne 安全响应头，替代 Cloudflare 的 `_headers`，因本项目选定 EdgeOne）。
- **S3（已落地）**：去第三方字体 → 系统字体栈。
- **S4**：INP 优化（拆分主线程长任务、交互时 yield、`font-display: swap`），上线后用 `web-vitals` 库做 RUM 监控。
- **S5**：备案决策（免费免备案海外/香港 vs 付费备案国内托管）。
- **S6（部分已落地）**：隐私政策 + 免责声明（页脚免责声明已加；独立隐私政策页待发布时补）。
- **S7**：每季度合规体检（境外脚本、新增算法功能、AI 生成内容标识）。
- **S8**：社媒分发（用封面工厂产出小红书/抖音/视频号/B站/公众号多尺寸图文短视频导国内流量）。
- **S9**：AEO 监控（每月手动测 10–20 个核心问题在 ChatGPT/Perplexity/Gemini/Bing 的引用）。
- **S10**：变现起步（先联盟，再知识付费/服务/广告）。

### 附录 D. 本轮已落地的代码/配置改动（2026-09-11）
- **`index.html`**：删除 `fonts.font.im` 三处字体 `<link>`（消除第三方请求与 PIPL 出境风险）；页脚新增"免责声明"段落（排名性质、仅供参考、商标归属）。
- **`styles.css`**：字体变量 `--d` / `--mono` 改为**系统字体栈**（去掉 Outfit/Noto Sans SC/JetBrains Mono 外部名），回退稳定、零延迟。
- **`build_standalone.py`**：新增从 `data.js` 解析 TOP50 → 生成 **JSON-LD（ItemList, 50 条）** 注入 `<head>`、生成 **`<noscript>` 纯文本榜单兜底** 注入 `#view`；更新文档说明为"零外部依赖"。
- **新增部署文件**（根目录，随单文件一同上传）：`llms.txt`、`robots.txt`、`sitemap.xml`、`edgeone.json`（EdgeOne 安全响应头：CSP / X-Content-Type-Options / Referrer-Policy / X-Frame-Options / Permissions-Policy，替代 Cloudflare 的 `_headers`）。
- **`dist/index.html`**：重新打包（200.8 KB，由 `build_standalone.py` 输出），已通过校验——50 条 JSON-LD、50 条 noscript、0 个跨站资源请求、本地资源全内联；`dist/` 现含 `index.html` + 四个脚手架文件，可直接作为 EdgeOne 的「输出目录」。

---

## 附录 C. 汇总整理的外部资料要点（权威来源，2026）
- ICP 备案流程与"接入商/实名"要求：腾讯云备案文档、阿里云备案帮助、时代互联《ICP 备案完整指南（2026）》、酷番云个人备案实务。
- Core Web Vitals 2026 阈值（LCP≤2.5s / INP≤200ms / CLS≤0.1，INP 取代 FID）：web.dev / HostingGuider / Prooflytics / RedLineSoft 多篇 2026 实测。
- AEO / GEO / llms.txt（ChatGPT 依赖 Bing+GPTBot、Perplexity 实时、AI Overviews 偏好 Top10、答案块/对比表/原创数据/实体一致性）：Stackmatix、ThePlanetTools、StraightGrowth、App-Lab、ThinkTech 2026 指南。
- PIPL 数据出境与 Google Fonts 风险（2026 网络安全法修订、出境认证办法、慕尼黑法院 GDPR 判例、自托管/Bunny Fonts 替代）：Exterro PIPL 综述、Nixon Digital 2026 研究、School4SEO、Pistack 自托管字体指南。
- 静态站 JS 渲染 SEO（两波索引、Bing/AI 爬虫弱于 Google、预渲染/SSG/noscript 兜底）：Static.app、BrightKeyword、Clarigital、WizardSEO、ShahidShahmiri 2026 SPA SEO。
- 网站盈利（AdSense 无门槛但 RPM 低、百度联盟需备案、Affiliate 国内外平台、按受众分层变现）：ZZ1984 老站长总结、MonetizeBetter、CSDN 内容变现、EastonDev 三路径对比。
- AI 合规"双备案"边界（算法备案 2–3 月 / 大模型备案 6–8 月 / 大模型登记省级审核；自研 vs 调 API 区分；生成式AI内容标识办法 2025-09）：君合、观韬、锦天城、腾讯云 2026 实操、汉坤。
- 免费托管实测（Cloudflare Pages 无限带宽免备案国内抽风；EdgeOne 国际站免费套餐回源香港国内较稳；GitHub Pages 国内慢）：2026 免费托管合集、Cloudflare 官方定价页、EdgeOne 免费套餐文档。
