# 免费方案：百度 SEO 优化 + 免费托管部署操作手册

> 生成日期：2026-09-12　｜　站点：https://model-hub-994.pages.dev
> 前提：**零成本**（不买域名、不买服务器）

---

## 一、已完成的 SEO 改造（代码层，已构建生效）

### 1.1 改动清单与验证结果

| 项目 | 改造前 | 改造后 | 验证 |
|---|---|---|---|
| **title** | 大模型一站式接入指南 · 按你的电脑选模型 + API 接入教程 | **2026大模型排行榜Top50_本地部署与API接入指南 - model-hub** | ✅ |
| **keywords** | **缺失** | 已注入 11 个核心词 | ✅ |
| **description** | 只讲"接入教程"，无"排名" | 重写，含"排行榜/Top50/三大榜单/显存档位/每月更新" | ✅ |
| **canonical** | 缺失 | `https://model-hub-994.pages.dev/` | ✅ |
| **OG 标签** | 缺失 | og:type / site_name / title / description / url / locale | ✅ |
| **语义化结构** | 仅 h1 + nav/main | 新增 `<article>` 包裹、2 个 `<h2>`、`<time datetime>` | ✅ |
| **结构化数据** | 已有 JSON-LD ItemList | 保留 | ✅ |
| **预渲染** | 已有 50 行榜单 | 保留（爬虫不执行 JS 也能读到全文） | ✅ |

**关键修复**：原来的 title 和 description 里**都没有"排名"二字**。
用户搜"大模型排名"时，你的页面在标题层面完全不匹配——这是之前最大的硬伤，现已修正。

### 1.2 现在的标题层级

```
h1  全球 Top50 大模型排名就在下面 / 再告诉你：你的电脑该装哪个
 ├─ h2  2026 年 9 月全球大模型排行榜 Top50        ← 新增
 │   ├─ time datetime="2026-09-11"                ← 新增
 │   ├─ 排序方法说明
 │   ├─ 筛选按钮
 │   └─ ol > li × 50（预渲染榜单全文）
 └─ h2  怎么读这个榜                                ← 新增
```

`<article>` 包裹整个榜单区块，让爬虫明确识别"这是一个独立的内容主体"。

### 1.3 一句实话：keywords 标签的作用

**百度和 Google 早已把 `meta keywords` 的权重降到极低**，它对排名基本没有直接影响。
我按要求加上了，也填的都是真实相关词（不是堆砌），但**别指望靠它提升排名**。
真正起作用的是：title、description、h 标签层级、正文内容、预渲染、结构化数据——这些都已经做了。

---

## 二、把链接交给百度（免验证，本次最重要）

### 2.1 百度免验证提交入口 ⭐

```
https://ziyuan.baidu.com/linksubmit/url
```

**这是绕开站长平台限制的关键**：

| 特性 | 说明 |
|---|---|
| 是否需要验证站点 | **不需要** |
| 是否需要自有域名 | **不需要** |
| 能否提交 pages.dev | **可以** |
| 每日额度 | 新站可能有额度限制，逐条提交即可 |

> 对比：`https://ziyuan.baidu.com/linksubmit/index` 是**需要站点验证**的入口（sitemap/API 提交），
> 你因为 pages.dev 主域配额超限走不通。**用 `/url` 这个入口，完全绕开这个问题。**

### 2.2 操作步骤

1. 打开 https://ziyuan.baidu.com/linksubmit/url ，登录百度账号
2. 粘贴：`https://model-hub-994.pages.dev/`
3. 完成图片验证 → 提交
4. **每周提交一次**，持续 3–4 周

### 2.3 其他搜索引擎的免验证入口

| 搜索引擎 | 入口 | 是否需要验证 |
|---|---|---|
| 百度 | https://ziyuan.baidu.com/linksubmit/url | **否** |
| 360 搜索 | https://info.so.360.cn/site_submit.html | **否** |
| 搜狗 | https://zhanzhang.sogou.com | 是 |
| 神马 | https://zhanzhang.sm.cn | 是 |
| 头条搜索 | https://zhanzhang.toutiao.com | 是 |
| Bing | https://www.bing.com/webmasters | 是（但 pages.dev 可直接验证，无配额限制） |

**百度 + 360 这两个免验证的先做掉**，零成本。

### 2.4 一个必须知道的限制

你的站是**单页应用（SPA）**，切换 tab 不改变 URL，所以**只有一个 URL 可被收录**：
`https://model-hub-994.pages.dev/`

好消息是：预渲染已经把 Top50 榜单全文直接写进了 HTML，百度抓首页就能读到全部 50 条的完整内容。
所以"只有一个 URL"对你的站**影响不大**——内容都在首页里。

> 如果未来想让"按硬件选""模型库"等 tab 各自被收录，需要拆成多页或配置 history 路由，
> 属于中期重构，本次不动。

---

## 三、免费托管平台部署

### 3.1 平台对比（针对百度收录这一诉求）

| 平台 | 免费额度 | 对百度蜘蛛 | 推荐度 |
|---|---|---|---|
| **Cloudflare Pages** | 无限站点/请求/带宽，500 次构建/月 | **不封禁**（实测 200 正常） | ⭐⭐⭐ **当前在用** |
| GitHub Pages | 1GB 存储 / 100GB 流量 | **403 封禁**（官方屏蔽） | ❌ 不要换 |
| Vercel | 100GB 流量 | 不封，但 **2024 起国内被墙** | ❌ 不要换 |
| Netlify | 100GB 流量 | 尚可 | ⭐⭐ 备选 |
| Gitee Pages | 需实名 | 国内友好 | ⭐⭐ 备选（国内镜像） |

**结论：你现在所在的 Cloudflare Pages 就是最优解**，不要换平台。
（实测：模拟 Baiduspider UA 访问返回 200 + 完整 230KB HTML，无拦截）

### 3.2 部署方式 A：GitHub 连接（推荐，当前在用）

```
1. 把代码推到 GitHub 仓库
2. Cloudflare Dashboard → Workers & Pages → Create → Pages → Connect to Git
3. 选择仓库
4. 构建配置：
     构建命令：留空（dist/ 已在仓库里，无需构建）
     输出目录：dist
5. Save and Deploy
```

之后每次 `git push` 自动触发部署。

### 3.3 部署方式 B：Wrangler CLI 直接上传（不想用 Git 时）

```bash
npm install -g wrangler
wrangler login
cd dist
npx wrangler pages deploy . --project-name=model-hub
```

### 3.4 部署方式 C：Dashboard 拖拽（最省事）

Cloudflare Dashboard → Pages → 选择项目 → **上传资产** → 把 `dist/` 整个文件夹拖进去。

### 3.5 必须确认的两项配置

**① 输出目录必须是 `dist/`**
`build_standalone.py` 会把所有东西打进 `dist/`：`index.html` + `robots.txt` + `sitemap.xml` + `llms.txt` + `_headers`。

**② 检查 Bot Fight Mode 是否关闭**
Cloudflare → Security → Bots → **Bot Fight Mode = Off**

> 这是最危险的一个坑：Baiduspider 虽在 Cloudflare 官方 Verified Bot 列表里，
> 但多方反馈 Bot Fight Mode 存在**误杀 Yandex/Baidu 蜘蛛的已知 Bug**。
> 一旦误开，百度蜘蛛被静默拦截，而你**因为没验证站点，看不到任何报错**。

---

## 四、验证与监控

### 4.1 提交后逐条检查

```
第 3 天   百度搜索框输入：site:model-hub-994.pages.dev
第 7 天   百度搜索框输入：model-hub
第 14 天  百度搜索框输入：大模型排名 2026
第 30 天  百度搜索框输入：2026大模型排行榜
```

### 4.2 时间预期（要现实）

| 阶段 | 预期 |
|---|---|
| 提交后 3–7 天 | 可能被抓取 |
| 2–4 周 | 首页可能进入索引 |
| 1–3 个月 | 长尾词开始有展现 |

**免费 + 无独立域名 + 新站**，这三重因素叠加，百度收录本来就慢。
要有耐心，同时配合内容分发（见《外链投放文案包》）加速蜘蛛发现。

### 4.3 验收标准

```bash
# 确认预渲染内容可被无 JS 环境读取
curl -A "Baiduspider" https://model-hub-994.pages.dev/ | grep -c "rank-row"
# 期望输出 50（说明 50 条榜单全部在 HTML 里）

# 确认 robots / sitemap 可访问
curl -I https://model-hub-994.pages.dev/robots.txt     # 期望 200
curl -I https://model-hub-994.pages.dev/sitemap.xml    # 期望 200
```

---

## 五、本次改动的文件

| 文件 | 改动 |
|---|---|
| `index.html` | head 全面重写：title / keywords / description / canonical / OG / robots / format-detection / renderer |
| `build_standalone.py` | 预渲染区块加 `<article>` 包裹、2 个 `<h2>`、`<time datetime>`、lab-intro 拆成两个 `<p>` |
| `dist/index.html` | 重新构建，231.1 KB |

---

## 六、参考资料

1. 百度免验证链接提交入口（官方教程，明确"可提交非验证站点的链接"）
   https://www.seoshipin.cn/baiduseo/baidupingtai/1944.html
2. 2026 各大搜索引擎提交入口整理（区分"需要权限/不需要权限"两类入口）
   https://www.st-lt.com/?p=2194/
3. 百度搜索资源平台链接提交通道说明
   https://yongqiang.blog.csdn.net/article/details/105906241
4. 百度搜索链接提交通道（含 `/linksubmit/url` 免验证用法）
   https://blog.csdn.net/q15976405716/article/details/113563849
5. Cloudflare Pages 免费层能力与自定义域配置
   https://overtone-digital.com/articles/cloudflare-pages-guide
6. Cloudflare 官方 · 拦截恶意机器人（Bot Fight Mode 无法被自定义规则跳过）
   https://developers.cloudflare.com/use-cases/solutions/stop-malicious-bots/
7. WebNots · Cloudflare 已知误杀 Yandex / Baidu 蜘蛛
   https://www.webnots.com/here-is-why-you-should-not-use-cloudflare-block-known-bots-feature/
